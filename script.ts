// Run: tsc script.ts
console.log('Hello TypeScript!');

function Hi(name: string, country: string) {
    console.log(`Hello ${name} from ${country}`);
}

Hi('John', 'USA');

console.log('ts installed!');

const car = {
    brand: 'BMW',
    engine: 3.0,
};

console.log(car.engine);

const adultness = age >= 18 ? 'adult' : 'minor';

switch (adultness) {
    case "adult":
}

